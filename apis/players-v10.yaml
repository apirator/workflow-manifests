apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: players-v10-0
  annotations:
    kubernetes.io/ingress.class: kong
spec:
  rules:
    - host: apis.apiriders.cloud
      http:
        paths:
          - backend:
              serviceName: players-v10
              servicePort: 80
            path: /v2/.*
